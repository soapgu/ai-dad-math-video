# 项目规则

## 媒体生成执行规范

- 无论使用内置生成工具、`arkcli` 或其他媒体工具，只允许向对话返回媒体对象、本地文件路径、短 URL 和必要元数据；禁止读取、打印、序列化或转发图片、视频、音频、PDF 等文件的二进制正文、Base64 数据或其他超大媒体数据到文本上下文。
- 调用内置图像生成工具后，必须将返回值作为图片结果处理，使用 `generatedImage(result)` 展示结果；禁止使用 `text(JSON.stringify(result))`、打印 `image_url`，或以任何文本形式输出 Base64 图片数据。
- 使用 `arkcli` 生成媒体时，以返回的 `local_path` 作为正式产物来源，只读取路径、短 URL、任务状态和必要错误信息；禁止使用 `cat`、`base64`、`xxd` 等命令将媒体正文写入 stdout 或对话上下文。
- 项目素材生成完成后，根据工具返回的 `output_hint` 定位文件，并立即复制到用户指定的项目路径；不得让已验收素材只停留在 `$CODEX_HOME/generated_images/`。
- 每次生成后只进行一次核心验收。满足用户明确的验收条件后立即落盘、更新相关计划文档并结束任务；未通过时针对一个明确问题进行修正，不进行无意义的重复检查或优化。
- 修正未通过验收的媒体时，遵循全局修复问题上报机制：从发现问题到修复调试超过 10 分钟，且同一问题已尝试两次仍无改善时，停止生成并汇报问题、修复过程和下一步方案。
- 验收时只检查用户明确指定的核心条件。轻微风格、构图或表情差异不影响核心要求时，不得擅自重新生成。
- 新一轮生成前，先检查上一轮工具返回的生成文件是否已经存在。用户要求继续收尾或使用已有图片时，优先复用现有生成结果，禁止重新生成。
- 若媒体已生成或展示，但任务出现超时、断线或重连迹象，恢复后先检查已有结果：结果完整且符合验收条件时，优先完成“定位已有文件、复制到项目、更新文档”流程；仅当结果不存在、损坏或不符合验收条件时才能重新生成。
- 生成过程明显超过该工具或任务的正常预期，或持续无状态变化时，向用户汇报当前状态；异步任务按任务状态轮询，不因等待时间较长而重复提交生成任务。

## 媒体素材验收与交付

- 媒体落盘后仅执行必要的文件检查：目标文件存在、格式正确、尺寸或时长符合要求。
- 最终回复必须提供项目内的绝对文件链接，并简要说明生成与修正情况以及验收结果。
- 未经用户明确要求，不删除 `$CODEX_HOME/generated_images/` 中的原始生成文件，不执行 Git 暂存或提交。
